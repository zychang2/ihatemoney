{% trans trimmed
         name=project.name
         id=project.id
         bill_url=url_for("main.list_bills", _external=True)
         invite_url=url_for("main.invite", _external=True)
%}
Hi,

You have just (or someone else using your email address) created the project "{{ name }}" to share your expenses.

You can access it here: {{ bill_url }} (the identifier is {{ id }}).
If you want to share this project with your friends, you can share the identifier and the shared password with them or send them invitations with the following link:
{{ invite_url }}

Enjoy,
{% endtrans %}
